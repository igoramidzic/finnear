import type { ToolSet } from "ai";

// Built-in tools available to every chat thread by default.
// Drop a new file in this directory and add it here to register it.
export const BUILTIN_TOOLS: ToolSet = {};
